#functions

//defining the function 

function logger(Parameter) {

console.log('My name is Miller");

};

logger(Argument );   
                     // calling/running/invoking
                  //the function 

Example:

function fruit processor (apples, oranges) {

console.log(apples, oranges);

const juice = ` Juice with $&apples & apples and $foranges & oranges.`;

return juice;

};

const applejuice = fruitprocessor (5,0);" console.log (applejuice);
